.panel.panel-default
  .panel-heading
    a data-toggle="collapse" data-parent="#grouop_list" href="##{group.id}" = "#{group.name} グループ"
  .panel-collapse.collapse id="#{group.id}"
    .panel-body.row
      .col-md-6
        h5 #{group.name} グループの説明
        p = group.description
      .col-md-6
        h5 メンバー
        - group.users.each do |user|
          p = user.name
